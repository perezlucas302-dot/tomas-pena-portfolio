/** Curva de easing compartida — la misma --ease-site que usa el resto del sitio. */
export const EASE = [0.22, 1, 0.36, 1] as const

/** Duración total de la entrada "Scale Brutal" (segundos). */
export const ENTRY_DURATION = 1.2
